# RAG service dependencies.
#
# Install:  pip install -r requirements.txt
#
# Some formats additionally need SYSTEM binaries that pip cannot install.
# See "Document formats" in the project README:
#   * Scanned PDFs (OCR)  -> Tesseract OCR
#   * Legacy .doc / .ppt  -> LibreOffice
# Without them the app still runs; those formats are reported as unavailable
# rather than silently failing.

# --- API service ------------------------------------------------------------
fastapi>=0.115
uvicorn>=0.30
python-dotenv>=1.0
pydantic>=2.7

# --- LLM / RAG chains -------------------------------------------------------
langchain>=1.3
langchain-core>=1.4
langchain-chroma>=1.1
langchain-community>=0.4
langchain-huggingface>=1.2
langchain-mistralai>=1.1
langchain-tavily>=0.2
langchain-text-splitters>=1.1
chromadb>=1.5

# --- Embeddings -------------------------------------------------------------
sentence-transformers>=5.0
# torch is intentionally not pinned: install the build matching your CUDA
# version from https://pytorch.org (verified on 2.11+cu128).

# --- Document extraction ----------------------------------------------------
pypdf>=6.0                 # PDF text layer
pymupdf>=1.24              # PDF page rendering for OCR (no poppler needed)
pytesseract>=0.3.10        # Tesseract wrapper - needs the tesseract binary
pillow>=10.0               # image handling for OCR
python-docx>=1.1           # .docx
python-pptx>=1.0           # .pptx
olefile>=0.46              # legacy OLE2 container sniffing

# --- Web research tools -----------------------------------------------------
beautifulsoup4>=4.13
requests>=2.32
rich>=13.0
