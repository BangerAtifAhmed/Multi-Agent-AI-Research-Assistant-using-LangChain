# Frontend configuration.
# Everything here is compiled into the browser bundle, so it must never
# contain secrets. DATABASE_URL, REDIS_URL, GOOGLE_CLIENT_SECRET, LLM keys and
# SESSION_SECRET all live in backend/.env only.

VITE_API_URL=http://localhost:3000/api
